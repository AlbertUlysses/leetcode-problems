class Solution:
    def reverse(self, x: int) -> int:
        temp = str(x)
        temp2 = []
        for i, e in enumerate(temp):
            e = int(e)
            temp2.append(e)
        return ans
        for i, e in enumerate(temp2):
            
            expo = 1^(len(temp2)) * e
            
